<mvc:View
	xmlns:mvc="sap.ui.core.mvc"
	controllerName="pg.kanban.controller.App"
	displayBlock="true"
	xmlns="sap.m">

	<App id="appIntermediateProduction"
		 busy="{appView>/busy}"
		 busyIndicatorDelay="{appView>/delay}"/>

</mvc:View>